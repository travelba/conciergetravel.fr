/**
 * Validated env for `apps/web-v2`.
 * Set `SKIP_ENV_VALIDATION=true` and `NEXT_PUBLIC_SKIP_ENV_VALIDATION=true`
 * in `apps/web-v2/.env.local` for local dev without real credentials.
 */
export { env } from '@mch/config/env/web';
